apiVersion: v1
kind: Service
metadata:
  name: {{ include "remla-chart.fullname" . }}
  labels:
    {{- include "remla-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remla-chart.selectorLabels" . | nindent 4 }}
